# Public release audit record

The contract 2.0.0 source tree is intended to contain:

- 15 newly authored synthetic input entries and 29-field public agent examples;
- 29-field generated and reviewed MockProvider fixtures;
- separate agent and final JSON Schemas plus manifest v2;
- complete prompt v2.0.0 snapshots with verified hashes;
- no private word list, spreadsheet, production output, rendered private prompt, raw transcript,
  credential, machine-specific path, or uncertain third-party data.

Run lint, type checking, tests, the offline demo, and `audit-public-release` before publication.
Automated PASS is a guardrail and does not replace manual rights, privacy, or accuracy review.
